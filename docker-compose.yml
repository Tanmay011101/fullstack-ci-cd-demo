version: '3.8'
services:
  backend:
    build: ./backend
    ports:
      - "5000:5000"
    container_name: backend-demo

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    container_name: frontend-demo
    depends_on:
      - backend